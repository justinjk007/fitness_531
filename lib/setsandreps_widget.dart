import 'package:flutter/material.dart';
import 'save_state.dart';

class FirstScreen extends StatelessWidget {
  @override
  Widget build(BuildContext ctxt) {
    return ListTile(
      title: Text("Warm up Set 1"),
      subtitle: Text("5 x 10 sets"),
    );
  }
}
